name: Release

on:
    push:
        branches: [ master ]

jobs:
    release:
        name: 'Release'
        runs-on: ubuntu-latest
        permissions:
            contents: write
            issues: write
            pull-requests: write
        steps:
            - name: Checkout
              uses: actions/checkout@v3
              with:
                  fetch-depth: 0
            - name: Setup Node.js
              uses: actions/setup-node@v3
              with:
                  node-version: '20'
            - name: Install dependencies
              run: npm install
            - name: Install plugins
              run: npm install --save-dev @semantic-release/release-notes-generator @semantic-release/git @semantic-release/commit-analyzer 
            - name: Release
              env:
                  GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}

              run: npx semantic-release