import { discord } from './discord.docs';
import { states } from './states.docs';
import * as BaseTypes from '../../@types/base';

const userDocs = {
    discord,
    states
};

export default userDocs;